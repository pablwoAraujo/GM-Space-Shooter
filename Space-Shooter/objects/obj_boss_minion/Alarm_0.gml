/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

// Avisando que está pronto para atirar
already = true;

// Criando o disparo
instance_create_layer(x, y, "Shots", obj_minion_shot);

// Redefinindo o alarme
alarm[0] = 30;
