/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

// Randomizando as cores
colors = choose(c_red, c_fuchsia, c_lime, c_aqua, c_teal);

// Alterando a escala da sprite
image_xscale = 4; 
image_yscale = 4;

// Tocando a explosão
audio_play_sound(snd_explosion, 2, false);
