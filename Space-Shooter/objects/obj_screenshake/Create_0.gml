/// @description Iniciando
// Você pode escrever seu código neste editor

// Intensidade do tremor
intensity = 10;

// Pegando a posição inicial da view port
view_x = camera_get_view_x(view_camera[0]);
view_y = camera_get_view_y(view_camera[0]);
