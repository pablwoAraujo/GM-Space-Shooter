/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

// Criando o target "alvo" do escudo
target = noone;

image_xscale = 1.2;
image_yscale = image_xscale;

image_alpha = 0.7;

// Som do escudo sendo criado
audio_play_sound(snd_shield_up, 1, 0);
